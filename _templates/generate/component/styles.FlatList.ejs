---
to: "<%= ['FlatList'].includes(componentType) ? `src/components/${folder}/${h.changeCase.pascal(name)}/styles.css` : null %>"
---
.contentContainer {
	flex: 1;
}
.itemContainer {
	flex-direction: row;
	padding: 16px;
}
.itemTitle {
	
}
.style {

}
