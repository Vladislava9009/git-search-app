---
to: "<%= ['SectionList'].includes(componentType) ? `src/components/${folder}/${h.changeCase.pascal(name)}/styles.css` : null %>"
---
.contentContainer {
	flex: 1;
}
.sectionHeaderContainer {
	flex-direction: row;
	padding: 16px;
}
.sectionHeaderTitle {
	
}
.itemContainer {
	flex-direction: row;
	padding: 16px;
}
.itemTitle {
	
}
.style {

}
